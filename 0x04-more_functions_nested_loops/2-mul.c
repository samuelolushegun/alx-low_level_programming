#include "main.h"

/**
 * mul - multiplies two integers
 *@a: first parameter
 *@b: second int parameter
 *
 * Return: returns the multiplication of the two parameters.
 */

int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
